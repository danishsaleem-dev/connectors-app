Drop the master app icon here as `app_icon.png` — 1024x1024, PNG.

Then run:

```
dart run flutter_launcher_icons
```

That regenerates every launcher/app-store icon size for both Android
(including the adaptive icon foreground/background) and iOS from this one
file — nothing else needs to be touched by hand.
